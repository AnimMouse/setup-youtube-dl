name: "Setup youtube-dl"
description: "Setup/Install youtube-dl for GitHub Actions"
branding:
  icon: "play-circle"
  color: "red"
  
runs:
  using: "composite"
  steps:
    - name: Install youtube-dl using pip3
      shell: bash
      run: sudo -H pip3 install youtube-dl